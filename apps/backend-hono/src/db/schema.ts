// Add Drizzle table definitions here as feature work is implemented.
// The D1 and Drizzle foundation is set up first so later tasks can own
// the actual application and auth schema design.

export {};
